CREATE TABLE drivers (id UUID PRIMARY KEY, status VARCHAR(32) NOT NULL, created_at TIMESTAMPTZ NOT NULL);
CREATE TABLE assignments (id UUID PRIMARY KEY, order_id UUID NOT NULL UNIQUE, driver_id UUID NOT NULL REFERENCES drivers(id), created_at TIMESTAMPTZ NOT NULL);
CREATE TABLE processed_events (event_id UUID PRIMARY KEY, processed_at TIMESTAMPTZ NOT NULL);
INSERT INTO drivers (id, status, created_at) VALUES
 ('00000000-0000-0000-0000-000000000001', 'AVAILABLE', now()),
 ('00000000-0000-0000-0000-000000000002', 'AVAILABLE', now());
