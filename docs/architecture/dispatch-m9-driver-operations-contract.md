# Dispatch — M9 Driver Operations Contract

M9 gives dispatch an internal driver-operations surface: operators can view drivers and explicitly mark an unassigned driver unavailable or available. Assignment remains the only path to `ASSIGNED`; an assigned driver cannot be taken offline through this control.
